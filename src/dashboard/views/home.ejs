<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard del Bot</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <nav class="bg-indigo-600 text-white p-4">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-2xl font-bold">Bot Dashboard</h1>
            <div>
                <% if (user) { %>
                    <a href="/dashboard" class="px-4 py-2 bg-indigo-500 rounded hover:bg-indigo-700">Dashboard</a>
                    <a href="/logout" class="px-4 py-2 bg-red-500 rounded hover:bg-red-700 ml-2">Cerrar Sesión</a>
                <% } else { %>
                    <a href="/login" class="px-4 py-2 bg-indigo-500 rounded hover:bg-indigo-700">
                        Iniciar Sesión con Discord
                    </a>
                <% } %>
            </div>
        </div>
    </nav>

    <main class="container mx-auto mt-8 p-4">
        <% if (!user) { %>
            <div class="text-center">
                <h2 class="text-4xl font-bold text-gray-800 mb-4">¡Bienvenido al Dashboard!</h2>
                <p class="text-xl text-gray-600 mb-8">Inicia sesión con Discord para gestionar el bot en tus servidores.</p>
                <a href="/login" class="inline-block px-8 py-4 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700 transition">
                    Iniciar Sesión con Discord
                </a>
            </div>
        <% } else { %>
            <div class="bg-white rounded-lg shadow p-6">
                <h2 class="text-2xl font-bold mb-4">¡Bienvenido, <%= user.username %>!</h2>
                <p class="text-gray-600 mb-4">Accede al dashboard para gestionar el bot en tus servidores.</p>
                <a href="/dashboard" class="inline-block px-6 py-3 bg-indigo-600 text-white rounded hover:bg-indigo-700 transition">
                    Ir al Dashboard
                </a>
            </div>
        <% } %>
    </main>

    <footer class="mt-8 py-4 bg-gray-200">
        <div class="container mx-auto text-center text-gray-600">
            <p>&copy; 2025 Bot Dashboard. Todos los derechos reservados.</p>
        </div>
    </footer>
</body>
</html>
